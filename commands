set -x
echo "$PATH"
pwd
ls -al /workspace
export PATH="$(pwd)/.venv/bin:$PATH"
echo "$PATH"
ansible-playbook -i inventory/inventory.yml ansible/plays/provision.yml --limit beelink -e "vault_root_password=$VAULT_TOKEN"
